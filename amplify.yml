version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --cache .npm --prefer-offline
    build:
      commands:
        # - npm run build -- --configuration=$ENV
        - npm run timo -- --configuration=$ENV
  artifacts:
    baseDirectory: dist/amplify-demo/browser
    files:
      - '**/*'
  cache:
    paths:
      - .npm/**/*