version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci --cache .npm --prefer-offline
    build:
      commands:
        - npm run deploy -- --configuration=${ENV:-dev}
  artifacts:
    baseDirectory: dist/amplify-demo/browser
    files:
      - '**/*'
  cache:
    paths:
      - .npm/**/*